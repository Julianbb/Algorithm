### 题目:<br>
输入两个链表，找出它们的第一个公共结点。

### 解法:<br>
1. 从后向前遍历，找到最后一个相同的结点便是，从后遍历可以利用栈
2. 先计算出两个链表的各自长度，然后计算出长度差，先让长的链表走“长度差”，然后两个链表同时遍历，第一个相同的结点便是



### 注意事项<br>
1. 在构造的时候由于链表是呈Y型，所以在释放的时候不能以链表的形式释放，否则会出现内存错误，应该先释放一个链表，另一个应该一个一个结点的释放
